index = 0;

while (index <= 10) {

    console.log(index);
    index += 2;   
}

// working in array

let arr = ["Atharv", "Radhika", "Gayatri"];

let i = 0;
while (i < arr.length) {
    console.log(arr[i]);
    i++;
}

// do while where we execute code first and then  check condition

let ido = 0;
do {
    console.log(ido);
    ido++;
} while (ido < 3);

// there is pre increment and post increment
// i++ is returning i first then increment it but ++i is increment value first and then returns
